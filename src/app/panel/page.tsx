"use client";

import AdminPage from "@/components/AdminPage";

/**
 * /admin — Standalone admin panel.
 * Has its own login gate and layout — NO customer Header/Footer.
 */
export default function Admin() {
  return <AdminPage />;
}
